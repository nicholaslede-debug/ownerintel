/**
 * Assessor (Image Mate / RPS) skeleton.
 * Some counties expose parcel & owner JSON endpoints; many require navigation.
 * Implement fetching parcel by APN and upsert Owner + Property rows.
 */
export async function fetchImageMateParcel(apn: string) {
  // Implement site-specific fetching and parsing.
  // Return normalized fields for Owner + Property + land/building characteristics.
  return null
}
